# Configure your AWS credentials and S3 bucket details below.
# Replace the placeholder values with your actual credentials.
# After updating, rename this file to config.ini before running the application.

[aws]
access_key = YOUR_ACCESS_KEY_ID
secret_key = YOUR_SECRET_ACCESS_KEY
region = us-east-1

[s3]
bucket_name = your-bucket-name

